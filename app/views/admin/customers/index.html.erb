<section class="inner">
	<div class="section_admin">
	  <h2 class="section-ttl">会員一覧</h2>
	  <%= render 'index', customers: @customers %>
	  <div class="center">
	  	<%= paginate @customers %>
	  </div>
	</div>
</section>